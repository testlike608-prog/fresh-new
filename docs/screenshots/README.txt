حط هنا صور الشاشات عشان تظهر في الـ README الرئيسي:

1. gui_status.png    ← صفحة Status من الواجهة (http://localhost:8000)
                       والسيرفر شغال والكاميرا متوصلة — دي أهم صورة.
2. capture_app.png   ← نافذة capture_app.py والكاميرا شغالة.

اختياري (لو حبيت تضيفهم في الـ README):
3. gui_settings.png  ← صفحة Settings.
4. gui_logs.png      ← صفحة Logs.
5. tuner.png         ← نافذة live_enhance_tuner.py بالسلايدرز.

طريقة أخذ سكرين شوت: Win + Shift + S ← ظلل النافذة ← احفظ بالاسم المطلوب هنا.
لو الأسماء مطابقة للي فوق، الصور هتظهر في README.md تلقائياً.
